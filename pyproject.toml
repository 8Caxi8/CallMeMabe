[project]
name = "callmemaybe"
version = "1.2"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pydantic",
    "flake8",
    "mypy",
	"llm_sdk",
	"accelerate"
]

[tool.uv.sources]
llm_sdk = { path = "llm_sdk" }